use "examples/example_struct.bk"
use "examples/math.bk"

extern run_server(str, int, ptr,) -> int

def count_to_1M(n: int) -> int do
	if n < 1_000_000 then do
		print(n)
		count_to_1M(n+1)
	end
end

def handler(req: str) -> str do
	print("OK")
	"hello"
end

run_server("127.0.0.1", 8000, handler)